<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="SampleMapper">
  <select id="selectSampleList"
    parameterType="sample.VO.SearchConditionVO" resultType="sample.VO.SampleVO">
    SELECT
    ID, NAME, DESCRIPTION, USE_YN, REG_USER
    FROM SAMPLE
    WHERE 1=1
    <if test="keyword != null and keyword != ''">
      <choose>
        <!-- LIKE의 %는 DB마다 방법이 다릅니다 -->
        <when test="searchType == 'ID'">
          AND ID LIKE '%'||#{keyword}||'%'
        </when>
        <when test="searchType == 'NAME'">
          AND NAME LIKE '%'||#{keyword}||'%'
        </when>
      </choose>
    </if>
    ORDER BY ID ASC
  </select>
	
	
	<insert id="insertSampleList" parameterType="sample.VO.SampleVO">
    INSERT INTO SAMPLE (
    ID
    , NAME
    , DESCRIPTION
    , USE_YN
    , REG_USER 
    )
    VALUES (
    #{id}
    , #{name}
    , #{description}
    , #{useYn}
    , #{regUser}
    )
  </insert>

  <update id="updateSampleList" parameterType="sample.VO.SampleVO">
    UPDATE SAMPLE
    SET
    ID=#{id}
    , NAME=#{name}
    , DESCRIPTION=#{description}
    , USE_YN=#{useYn}
    WHERE ID=#{id}
  </update>

  <delete id="deleteSampleList" parameterType="sample.VO.SampleVO">
    DELETE FROM SAMPLE
    WHERE ID=#{id}
  </delete>
</mapper>